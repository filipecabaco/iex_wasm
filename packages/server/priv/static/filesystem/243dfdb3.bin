\ A slightly optimized version of an original C program published
\ at AVAST YE SWABS by XDpirate for the Ben Nanonote

include lib/choose.4th

." Your randomly generated password is:" cr
8 0 do
  62 choose dup 26 < if 65 + else 22 + dup 57 > if 39 + then then emit
loop cr